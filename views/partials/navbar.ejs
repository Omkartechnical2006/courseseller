<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
            <% if (locals.paymentSettings && paymentSettings.showLogo && paymentSettings.logoUrl) { %>
                <img src="<%= paymentSettings.logoUrl %>" alt="<%= paymentSettings.logoText %>" height="30" class="me-2">
            <% } %>
            <%= locals.paymentSettings && paymentSettings.logoText ? paymentSettings.logoText : 'Course Store' %>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">All Courses</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                <li class="nav-item me-3">
                    <a class="nav-link" href="<%= locals.paymentSettings && paymentSettings.telegramSupport ? paymentSettings.telegramSupport : 'https://t.me/course_provider01' %>" target="_blank" title="Get Support">
                        <i class="bi bi-headset"></i> Support
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/cart">
                        <i class="bi bi-cart-fill"></i> Cart
                        <% if (locals.cartItemCount && cartItemCount > 0) { %>
                            <span class="badge bg-danger"><%= cartItemCount %></span>
                        <% } %>
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav> 