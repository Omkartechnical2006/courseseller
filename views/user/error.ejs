<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - <%= error.title || 'Transaction Error' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        .error-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 40px 20px;
        }
        .error-icon {
            font-size: 60px;
            color: #dc3545;
            margin-bottom: 20px;
        }
        .error-actions {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <div class="error-container text-center">
            <i class="bi bi-exclamation-triangle-fill error-icon"></i>
            <h1 class="mb-4"><%= error.title || 'Transaction Error' %></h1>
            <div class="alert alert-danger">
                <%= error.message %>
            </div>
            <p class="text-muted">
                <%= error.description || 'There was an error processing your transaction. Please check the details below or contact support for assistance.' %>
            </p>
            
            <% if (error.showSupport) { %>
                <div class="alert alert-info mt-4">
                    <h5><i class="bi bi-headset me-2"></i>Need Help?</h5>
                    <p>If you've already made a payment and are seeing this error, please contact our support team with your transaction details:</p>
                    <div class="d-flex justify-content-center gap-3 mt-3">
                        <a href="<%= locals.paymentSettings && paymentSettings.telegramSupport ? paymentSettings.telegramSupport : 'https://t.me/course_provider01' %>" class="btn btn-outline-primary" target="_blank">
                            <i class="bi bi-telegram me-2"></i>Telegram Support
                        </a>
                        <a href="mailto:<%= locals.paymentSettings && paymentSettings.emailSupport ? paymentSettings.emailSupport : 'support@courseprovider.com' %>" class="btn btn-outline-primary">
                            <i class="bi bi-envelope me-2"></i>Email Support
                        </a>
                    </div>
                </div>
            <% } %>
            
            <div class="error-actions">
                <a href="javascript:history.back()" class="btn btn-outline-primary me-2">
                    <i class="bi bi-arrow-left me-2"></i>Go Back
                </a>
                <a href="/" class="btn btn-primary">
                    <i class="bi bi-house me-2"></i>Home
                </a>
            </div>
        </div>
    </div>

    <footer class="bg-light text-center text-muted py-4 mt-5">
        <div class="container">
            <p>&copy; 2023 Course Store. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 